apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied after base and monitoring: an HPA whose metric nothing serves yet reads
# <unknown> until prometheus-adapter is up.
namespace: gochat

resources:
  - hpa-connect-ws.yaml
  - hpa-api.yaml
